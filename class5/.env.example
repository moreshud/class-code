# cp .env.example .env  — never commit .env

# SSH target — no "ssh" prefix
export LAMBDA=ubuntu@YOUR_LAMBDA_IP

# Private key registered in Lambda console → SSH keys (must match instance launch key)
export LAMBDA_SSH_KEY=$HOME/.ssh/YOUR_LAMBDA_KEY

# Hugging Face — no space after =
export HF_TOKEN=
